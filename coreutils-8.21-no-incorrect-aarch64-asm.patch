--- coreutils-8.21/src/longlong.h.bero	2013-02-21 15:06:28.828261659 +0100
+++ coreutils-8.21/src/longlong.h	2013-02-21 15:08:09.534999210 +0100
@@ -529,10 +529,12 @@ extern UWtype __MPN(udiv_qrnnd) (UWtype
 #endif /* __arm__ */
 
 #if defined (__aarch64__) && W_TYPE_SIZE == 64
+#if 0 // usage of adc seems to be incorrect
 #define add_ssaaaa(sh, sl, ah, al, bh, bl) \
   __asm__ ("adds\t%1, %4, %5\n\tadc\t%0, %2, %3"			\
 	   : "=r" (sh), "=&r" (sl)					\
 	   : "r" (ah), "rZ" (bh), "%r" (al), "rI" (bl) __CLOBBER_CC)
+#endif
 #define sub_ddmmss(sh, sl, ah, al, bh, bl) \
   do {									\
     if (__builtin_constant_p (bl))					\
